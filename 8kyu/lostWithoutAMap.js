//BEGINNER - LOST WITHOUT A MAP
// Given an array of integers, return a new array with each value doubled.

// For example:

// [1, 2, 3] --> [2, 4, 6]

//MY SOLUTION
const lost = arr => arr.map(x => x*2);

//OTHER CLEVER SOLUTION
function maps(x){
    return x.map(n => n * 2);
}